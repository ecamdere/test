<?xml version="1.0" encoding="UTF-8"?>

<phpunit bootstrap="${basedir}/src/autoload.php" 
        backupGlobals="false" backupStaticAttributes="false" 
        strict="true" verbose="true">

    <testsuites>
        <testsuite name="ProjectName">
            <directory suffix="Test.php">${basedir}/tests/unit/</directory>
            <directory suffix="Test.php">${basedir}/tests/integration/</directory>
        </testsuite>
    </testsuites>

    <logging>
        <log type="coverage-html" target="${basedir}/build/coverage" title="My Project" 
                charset="UTF-8" yui="true" highlight="true" 
                lowUpperBound="35" highLowerBound="70" />
        <log type="coverage-clover" target="${basedir}/build/logs/clover.xml" />
        <log type="coverage-crap4j" target="${basedir}/build/logs/crap4j.xml"/>
        <log type="junit" target="${basedir}/build/logs/junit.xml" logIncompleteSkipped="false" />
    </logging>

    <filter>
        <whitelist addUncoveredFilesFromWhitelist="true">
            <directory suffix=".php">src</directory>
            <exclude>
                <file>src/bootstrap.php</file>
            </exclude>
        </whitelist>
    </filter>

</phpunit>